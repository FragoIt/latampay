# ============================================
# LatamPay Environment Variables Template
# --------------------------------------------
# Copia este archivo a `.env.local` y completa
# los valores antes de levantar el proyecto.
# ============================================

# --------------------------------------------
# Database
# --------------------------------------------
# URL de conexión completa a la base de datos PostgreSQL.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/latampay
# Tamaño máximo del pool de conexiones (número entero).
DATABASE_POOL_SIZE=10

# --------------------------------------------
# Blockchain
# --------------------------------------------
# Endpoint RPC para la red principal de Polygon.
POLYGON_RPC_URL=https://polygon-rpc.com
# Endpoint RPC para la red de pruebas Mumbai.
POLYGON_MUMBAI_RPC_URL=https://polygon-mumbai-bor.publicnode.com
# Clave privada del deployer (sin exponer en producción).
PRIVATE_KEY_DEPLOYER=0x
# Dirección del contrato Gateway desplegado en la red.
CONTRACT_ADDRESS_GATEWAY=0x0000000000000000000000000000000000000000
# Dirección del contrato USDC en la red seleccionada.
CONTRACT_ADDRESS_USDC=0x0000000000000000000000000000000000000000
# Dirección del contrato USDT en la red seleccionada.
CONTRACT_ADDRESS_USDT=0x0000000000000000000000000000000000000000

# --------------------------------------------
# Auth
# --------------------------------------------
# URL del proyecto Supabase.
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
# Clave pública (anon key) de Supabase.
SUPABASE_ANON_KEY=your-anon-key
# Clave de servicio de Supabase (no exponer en frontend).
SUPABASE_SERVICE_KEY=your-service-key
# Secreto usado para firmar JWTs (mínimo 32 caracteres).
JWT_SECRET=super-secret-key-change-me

# --------------------------------------------
# Payments
# --------------------------------------------
# Clave secreta de Stripe (empieza con sk_).
STRIPE_SECRET_KEY=sk_test_
# Secreto del webhook de Stripe (empieza con whsec_).
STRIPE_WEBHOOK_SECRET=whsec_

# --------------------------------------------
# External APIs
# --------------------------------------------
# API Key de Alchemy para conectarse a servicios blockchain.
ALCHEMY_API_KEY=alchemy-api-key
# API Key de CoinGecko para obtener precios de criptomonedas.
COINGECKO_API_KEY=coingecko-api-key
# API Key de CoinMarketCap para reportes de gas (opcional pero recomendado).
COINMARKETCAP_API_KEY=

# --------------------------------------------
# Verification
# --------------------------------------------
# API Key de PolygonScan para verificar contratos.
POLYGONSCAN_API_KEY=
# Dirección que será dueña del Gateway tras el deploy (opcional, usa el deployer por defecto).
GATEWAY_OWNER_ADDRESS=
# Activa verificación automática al desplegar (true/false).
AUTO_VERIFY=false

# --------------------------------------------
# App
# --------------------------------------------
# Entorno actual de la aplicación (development, production o test).
NODE_ENV=development
# Puerto donde se levantará el API.
PORT=3001
# URL del frontend que consumirá el API.
FRONTEND_URL=http://localhost:3000
